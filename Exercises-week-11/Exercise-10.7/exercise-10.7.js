function setOdd (){
    document.querySelectorAll(".odd").forEach(Element => {Element.innerHTML = "X"});
}

function setEven(){
    document.querySelectorAll(".even").forEach(Element => {Element.innerHTML = "O"})
}